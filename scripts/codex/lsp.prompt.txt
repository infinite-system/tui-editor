You are implementing ONE module of the Invar TUI code workspace: a fast terminal code editor on Bun + ivue + OpenTUI + Tree-sitter + git. You are one of several codex workers building disjoint modules in parallel, each in its own git worktree. Work ONLY in your worktree (your cwd), on your module.

== READ IN FULL BEFORE CODING (do not skim — this is the context you need to do it well) ==
At your worktree root:
- INVAR_TUI_EDITOR_BUILD_BRIEF_FINAL.md — the WHOLE brief (product thesis, scope, the 37 architectural invariants, verification, your module's milestone section). Read all of it.
- project.invariants.md — ALL project invariants (the generators your module must uphold) + project.lattice.md (how they compose).
- project.ivue-reference.md — the ivue implementation reference incl. the FLYWEIGHT pattern (copy it for any high-cardinality data). AUTHORITATIVE for ivue code.
- project.decisions.md (the 10 ivue decisions), project.architecture.md, project.implementation-plan.md (§3 module/file conventions, §4 milestones).
- .claude/skills/ivue/SKILL.md — the ivue skill.
Method skills (absolute paths — read them so you write proper invariant contracts by the IBR method):
- /home/parallels/dev/ibr/.claude/skills/invariants/SKILL.md — the /invariants skill (the contract schema, kinds, and how invariants are written/verified). Your <MODULE>.invariants.md MUST follow it.
- /home/parallels/dev/ibr/.claude/skills/ibr/SKILL.md and /home/parallels/dev/ibr/IBR/IBR.md — the /ibr skill + framework (invariant-based reasoning: reduce to load-bearing generators, sort reality vs chosen, predict impossibilities).
Existing code (read to learn interfaces; DO NOT modify): src/modules/{system,app,kernel,editor,workspace,theme,syntax} — Buffer/editor API, system capabilities, Theme, syntax highlighter.

== ARCHITECTURE YOU MUST FOLLOW ==
ivue owns observable state; OpenTUI owns terminal drawing/input; data flows one way: input -> model method -> mutation -> reactive invalidation -> requestRender.

Three class kinds:
- Reactive domain model (observable, identity, disposable): the DEFAULT for stateful models.
- Plain class (algorithms/resources, non-reactive): parsers, transports, process handles, engines.
- Static capability class (stateless backend ops): file/process/git command wrappers.

ivue facts (from project.decisions.md — do not get these wrong):
- ivue's ONLY public export is `Reactive()`. `ref`/`shallowRef`/`computed`/`watch`/`watchEffect` come from `vue` (already installed). Import reactivity from 'vue', wrappers from 'ivue'.
- `Static()` and the kernel are NOT in the ivue package (vendored elsewhere in the app). If your module needs a static capability, write it as a normal class with static methods for now (a plain `namespace X { const $Class=$X; let Class=$X }`), do not import Static from ivue.

The namespace pattern (EXACT shape for every class file, PascalCase.ts):
```ts
import { Reactive } from 'ivue';
import { ref, shallowRef } from 'vue';
class $Thing {
  get items() { return shallowRef<Row[]>([]); }   // ref-returning getter = mutable reactive state (.value)
  get count() { return this.items.value.length; }  // PLAIN getter = cheap derived state (reactive, 0 bytes/instance) — DO NOT use computed() for this
  add(r: Row) { this.items.value = [...this.items.value, r]; }
  dispose() { /* close handles first */ (this as any).$stopEffects?.(); }
}
export namespace Thing {
  export const $Class = $Thing;             // raw, immutable, children `extends` this
  export let Class = Reactive($Class);       // mutable selection slot; consumers do `new Thing.Class()`
  export type Model = InstanceType<typeof Class>;
  export type Instance = typeof Class.Instance;
}
```
Rules: cross-module deps read LATE via getters (`get Dep(){ return Dep.Class }`) — never top-level `new ImportedClass()` or `const C = X.Class`. Long-lived effects use `this.$watch`/`this.$watchEffect` and an owner calls `this.$stopEffects()` on dispose. Plain classes use the same namespace but `let Class = $Class` (no Reactive wrapper). Role-collection files (not one class) are named <module>.<role>.ts. NO HTTP roles (no controllers/routes/services).

Performance invariants that bind your module:
- Cost tracks the OBSERVED set: high-cardinality data (git commit history, markdown tokens, diagnostics) uses compact records/typed arrays + virtualized visible rows + eviction — NEVER one reactive object per commit/token/line.
- Async results (git refresh, LSP responses, parses) can outlive the state they describe: stamp every async result with a revision/version and DISCARD stale ones; the immediate UI must NEVER block on a subprocess.
- LSP/git are separate failable processes: lazy start, dispose on close, degrade gracefully if absent/slow/crashed. Editing must never freeze or crash on their failure.

== HARD RULES (violating any fails the task) ==
- Write ONLY files under src/modules/<MODULE>/ (and its __tests__/). Do NOT modify any other module, main.ts, package.json, or the root project.*.md docs. Integration/wiring into the app is the orchestrator's job, not yours.
- You MAY git add/commit within your worktree branch (that is expected). Do NOT delete files you did not create. Do NOT touch other worktrees.
- Install nothing.

== DELIVER ==
1. The module code implementing the scope below, following the patterns above.
2. src/modules/<MODULE>/<MODULE>.invariants.md — canonical schema (`## Reality-based invariants` then `## Chosen invariants`; each record: **Invariant** one if-then, **Scope**, **Mechanism**, **Generates**, **Evidence**, **Impossible if true**, **Verification**, **Status:** provisional, **Last refined:** 2026-07-21; names are unnumbered plain declarative sentences). Module invariants stand on project.invariants.md.
3. Tests under __tests__/ that pass with `bun test src/modules/<MODULE>`.
VERIFY before finishing: `bun test src/modules/<MODULE>` passes and `bunx tsc --noEmit` reports no new errors from your files. Then `git add -A && git commit`. In your final message, list files created, interface assumptions about other modules, and any invariant you could not yet verify.

== YOUR MODULE AND SCOPE ==

== EXISTING INTERFACES (already built — READ these files; do NOT reinvent or modify them) ==
Buffer/text — src/modules/editor/TextDocument.ts → `TextDocument.Model`:
  get revision(): Ref<number>; get dirty(): Ref<boolean>; get binary(); get text(): string; get lineCount(): number;
  line(i): string; lines: readonly string[]; slice(start,count): string[]; get eol(): '\n'|'\r\n';
  loadFromFile(path); loadFromText(text,path); setLine/insertLine/removeLine; insertInline(line,col,str); splitLine(line,col); markSaved().
  → markdown & lsp: assume the document you consume is a `TextDocument.Model` (use `.text`, `.revision.value`, `.line(i)`). Do NOT invent a different buffer shape.
Editor — src/modules/editor/Editor.ts → openFile(path), insertText(str), insertNewline(), backspace(), deleteChar(), performUndo/Redo(), save(): boolean, get title(), move*(); holds document+cursor+viewport.
Cursor — get line/col/goalCol; set(line,col). Viewport — get scrollTop/scrollLeft/height/width, firstVisible, visibleCount, setSize, scrollToLine, scrollBy.
system (static capability namespaces, call as `Files.Class.read(...)` or per existing usage) — 
  Files: exists/isDir/basename/extname/dirname/join/relative/list(dir):DirEntry[]/read/readBytes.
  Clock: now()/freeze(). Environment: env(key). Logging: info/error/write. Processes (src/modules/system/Processes.ts — use for subprocess spawning; read it). 
  StatusChannel: update(patch)/flush()/settle(frame) — the tmux observability side-channel (writes artifacts/status.json); call update() when your module changes user-visible state.
theme — src/modules/theme/Theme.ts → Theme.Model: get palette/icons/colorDepth/glyphLevel, icon(name,isDir,open), setPalette/toggleDark. theme.palettes.ts: Palette type, DARK/LIGHT/PALETTES, quantizePalette(p,depth).
OpenTUI (only markdown needs it, for MarkdownRenderable) — import { BoxRenderable, TextRenderable } from '@opentui/core'; construct `new BoxRenderable(renderer, { flexGrow, flexDirection, width, height, padding })`, `new TextRenderable(renderer, { id, content })`; call renderer.requestRender(); see src/modules/ui/RootView.ts for the real usage pattern. For a scrolling custom viewport, check @opentui/core exports (ScrollBoxRenderable, Renderable base) and follow RootView.
Tests — `import { test, expect } from 'bun:test';` files in __tests__/*.test.ts; construct models via `new X.Class()` (or `new X.$Class()` for a raw instance in a unit test); run `bun test src/modules/<MODULE>`.

== DON'T (each is a real failure mode) ==
- Do NOT import ref/shallowRef/computed/watch/watchEffect/effectScope from 'ivue' — they come from 'vue'. From 'ivue' you import ONLY `Reactive`.
- Do NOT import `Static` from 'ivue' (not exported). A static-capability class is a normal class in a namespace with `let Class = $Class` (no wrapper), matching src/modules/system/*.
- Do NOT create one reactive object per commit / token / diagnostic / line — compact records/arrays + a viewport view + revision refs. Cost tracks the OBSERVED set.
- Do NOT block on a subprocess (git/LSP) on any path the UI awaits — async, non-blocking, stale-superseded by revision/id.
- Do NOT snapshot `X.Class` in a module-scope const, top-level `new`, or default export — read deps late in getters/methods.
- Do NOT use `computed()` for cheap derived state — named plain getter. Reserve computed() for expensive/memoized derivations only.
- Do NOT modify or assume a different shape for the existing interfaces above — match them exactly.
MODULE: lsp   (brief milestone M5; TypeScript LSP client stack, multi-provider)
Files (write under src/modules/lsp/):
- JsonRpc.ts — plain class: LSP JSON-RPC framing (Content-Length) encode/decode over a byte stream; correlate request/response by id; handle partial + multiplexed messages in one chunk.
- LspTransport.ts — plain class: stdio transport over a child process stdin/stdout using JsonRpc.
- LspProcess.ts — plain class: spawn/own an LSP subprocess (Bun.spawn), lifecycle, dispose (kill+close). Never throw into the editor on spawn failure — expose an error state.
- LanguageProvider.ts — interface/types: id, command+args resolution, capabilities.
- TypeScriptProvider.ts — implements LanguageProvider; order vtsls -> typescript-language-server (detect availability; no hard-fail if absent).
- LanguageClient.ts — Reactive model. LAZY start (only when a semantic feature is requested / TS-JS file opens). initialize handshake; publishDiagnostics REVISION-STAMPED (tie to document version, discard stale); definition; hover + references if straightforward. LSP positions are UTF-16 — convert at the boundary. Disposable: shut server down on dispose. Survives server absence/crash without crashing the editor.
Load-bearing: lazy + disposable + revision-stamped + survives-failure. Do NOT wire into the editor; expose a clean client API. Tests: JSON-RPC framing (encode/decode, partial + multiplexed chunks); lifecycle (lazy start, dispose kills process) using a FAKE in-process scripted stdin/stdout server (do not require a real LSP binary); stale-diagnostic discard by version.
