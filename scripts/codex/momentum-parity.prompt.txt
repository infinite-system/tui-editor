You are a scoped worker on the Fable TUI editor (Bun + ivue + OpenTUI). Work ONLY in your current directory (a git worktree). Do NOT git commit (the reviewer commits). Do NOT delete files you did not create. Do NOT modify src/modules/ui/scroll-momentum.ts (proven physics — REUSE it). Use FULL DESCRIPTIVE identifier names — NO abbreviations ever (hard project convention: editor not ed, workspace not ws, index not i).

READ FIRST (worktree root): HANDOFF.md · PROGRESS.md ("USER PIPELINE" item 7 = your task) · src/modules/ui/scroll-momentum.ts (addImpulse/stepMomentum/halt/isMoving/AT_REST) · THE REFERENCE PATTERN = commit-log momentum: src/modules/workspace/GitPanel.ts (logMomentum shallowRef), src/modules/workspace/Workspace.ts (impulseGitLog/tickGitLogScroll/haltGitLogScroll — One-Writer halts on keyboard/jumps), src/modules/app/Bootstrap.ts onFrame (dt-clamped tick, self-sustaining requestRender) · src/modules/ui/RootView.ts onMouseScroll handlers (editor + sidebar wheel routing incl the horizontal branch and the changes-region branch) · src/modules/editor/Viewport.ts (scrollBy/scrollByColumns) · .claude/skills/invariants/SKILL.md.

TASK — momentum glide parity on ALL remaining panes, replicating the commit-log pattern EXACTLY:
1. Editor VERTICAL: wheel over the editor adds an impulse (momentum state on Viewport or Editor — mirror GitPanel.logMomentum's shape); the Bootstrap onFrame tick advances it via viewport.scrollBy (whole rows; totalLines clamp); keyboard cursor movement and scrollbar-thumb drags HALT it (One-Writer — halt in Viewport.scrollToLine and in the editor bar onChange).
2. Editor HORIZONTAL: same for scrollByColumns (impulse from the horizontal wheel branch — native left/right AND shift+vertical; clamp to the widest VISIBLE line exactly as the current direct handler does).
3. File tree: wheel adds impulse; tick moves tree.moveSelection by whole rows; keyboard arrows halt.
4. Git changes list: wheel over the changes region adds impulse; tick moves changesScrollTop (clamped to rows-length window as the current direct handler does); keyboard/thumb halt.
5. GENERALIZE the tick: Workspace.tickScrollAnimations(dtSeconds): boolean — ticks git log + editor V + editor H + tree + changes; Bootstrap onFrame calls ONLY this (replacing the git-log-specific call; keep the drag-auto-scroll + viewport-convergence lines untouched). Keep the dt clamp + self-sustaining requestRender semantics.
CRITICAL FEEL NOTE (user regression, pipeline item 11): the commit-log glide recently felt "not smooth" — while porting, ensure every wheel notch goes through addImpulse (NEVER a direct scrollTop step on the wheel path) and the tick runs EVERY frame while any momentum is moving. Do not retune the physics constants (the coordinator owns tuning).

VERIFY (all required; per-session side channels: scripts/tui-harness.sh launches with artifacts/status-<session>.json; `field <session> <name>`): `bunx tsc --noEmit; echo TSC=$?` → 0 · `bun test` all pass (port the Workspace.scroll.test.ts style for the new tick paths — unit-test tickScrollAnimations with injected momentum states) · checker `node .claude/skills/invariants/scripts/check_invariants.mjs --all --refs` → 0 · live tmux on a 1200-line scratch file (generate under /tmp): wheel-flick over the editor → editorScrollTop keeps advancing AFTER input stops then settles (sample the field over ~1.5s like the glide verification in git history 158ce95); same for shift+wheel horizontal (editorScrollLeft), tree (treeSelected), and changes list in a scratch git repo (changesScrollTop — create /tmp scratch repo; do NOT touch this repo's working tree). Run `bash scripts/smoke-editor.sh` — must stay ALL-PASS.

REPORT (final message): files changed, where momentum state lives per pane, the tick composition, glide transcripts (field samples over time per pane), and any halt site you added.
