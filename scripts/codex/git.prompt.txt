You are implementing ONE module of the Fable TUI code workspace: a fast terminal code editor on Bun + ivue + OpenTUI + Tree-sitter + git. You are one of several codex workers building disjoint modules in parallel, each in its own git worktree. Work ONLY in your worktree (your cwd), on your module.

== READ FIRST (context; the project docs are at the repo root of your worktree) ==
project.invariants.md, project.decisions.md (the 10 ivue architecture decisions — AUTHORITATIVE), project.architecture.md, project.implementation-plan.md (§3 module/file conventions, §4 milestones), FABLE_TUI_EDITOR_BUILD_BRIEF_FINAL.md (your module's section), and .claude/skills/ivue/SKILL.md. Also read the already-built modules under src/modules/{system,app,kernel,editor,workspace,theme} to learn the existing interfaces you depend on (Buffer/editor API, system capabilities, Theme). Do not modify them.

== ARCHITECTURE YOU MUST FOLLOW ==
ivue owns observable state; OpenTUI owns terminal drawing/input; data flows one way: input -> model method -> mutation -> reactive invalidation -> requestRender.

Three class kinds:
- Reactive domain model (observable, identity, disposable): the DEFAULT for stateful models.
- Plain class (algorithms/resources, non-reactive): parsers, transports, process handles, engines.
- Static capability class (stateless backend ops): file/process/git command wrappers.

ivue facts (from project.decisions.md — do not get these wrong):
- ivue's ONLY public export is `Reactive()`. `ref`/`shallowRef`/`computed`/`watch`/`watchEffect` come from `vue` (already installed). Import reactivity from 'vue', wrappers from 'ivue'.
- `Static()` and the kernel are NOT in the ivue package (vendored elsewhere in the app). If your module needs a static capability, write it as a normal class with static methods for now (a plain `namespace X { const $Class=$X; let Class=$X }`), do not import Static from ivue.

The namespace pattern (EXACT shape for every class file, PascalCase.ts):
```ts
import { Reactive } from 'ivue';
import { ref, shallowRef } from 'vue';
class $Thing {
  get items() { return shallowRef<Row[]>([]); }   // ref-returning getter = mutable reactive state (.value)
  get count() { return this.items.value.length; }  // PLAIN getter = cheap derived state (reactive, 0 bytes/instance) — DO NOT use computed() for this
  add(r: Row) { this.items.value = [...this.items.value, r]; }
  dispose() { /* close handles first */ (this as any).$stopEffects?.(); }
}
export namespace Thing {
  export const $Class = $Thing;             // raw, immutable, children `extends` this
  export let Class = Reactive($Class);       // mutable selection slot; consumers do `new Thing.Class()`
  export type Model = InstanceType<typeof Class>;
  export type Instance = typeof Class.Instance;
}
```
Rules: cross-module deps read LATE via getters (`get Dep(){ return Dep.Class }`) — never top-level `new ImportedClass()` or `const C = X.Class`. Long-lived effects use `this.$watch`/`this.$watchEffect` and an owner calls `this.$stopEffects()` on dispose. Plain classes use the same namespace but `let Class = $Class` (no Reactive wrapper). Role-collection files (not one class) are named <module>.<role>.ts. NO HTTP roles (no controllers/routes/services).

Performance invariants that bind your module:
- Cost tracks the OBSERVED set: high-cardinality data (git commit history, markdown tokens, diagnostics) uses compact records/typed arrays + virtualized visible rows + eviction — NEVER one reactive object per commit/token/line.
- Async results (git refresh, LSP responses, parses) can outlive the state they describe: stamp every async result with a revision/version and DISCARD stale ones; the immediate UI must NEVER block on a subprocess.
- LSP/git are separate failable processes: lazy start, dispose on close, degrade gracefully if absent/slow/crashed. Editing must never freeze or crash on their failure.

== HARD RULES (violating any fails the task) ==
- Write ONLY files under src/modules/<MODULE>/ (and its __tests__/). Do NOT modify any other module, main.ts, package.json, or the root project.*.md docs. Integration/wiring into the app is the orchestrator's job, not yours.
- You MAY git add/commit within your worktree branch (that is expected). Do NOT delete files you did not create. Do NOT touch other worktrees.
- Install nothing.

== DELIVER ==
1. The module code implementing the scope below, following the patterns above.
2. src/modules/<MODULE>/<MODULE>.invariants.md — canonical schema (`## Reality-based invariants` then `## Chosen invariants`; each record: **Invariant** one if-then, **Scope**, **Mechanism**, **Generates**, **Evidence**, **Impossible if true**, **Verification**, **Status:** provisional, **Last refined:** 2026-07-21; names are unnumbered plain declarative sentences). Module invariants stand on project.invariants.md.
3. Tests under __tests__/ that pass with `bun test src/modules/<MODULE>`.
VERIFY before finishing: `bun test src/modules/<MODULE>` passes and `bunx tsc --noEmit` reports no new errors from your files. Then `git add -A && git commit`. In your final message, list files created, interface assumptions about other modules, and any invariant you could not yet verify.

== YOUR MODULE AND SCOPE ==
MODULE: git   (brief milestone M4; git integration, no diff UI here)
Files (write under src/modules/git/):
- GitCommands.ts — capability class (namespace with `let Class = $GitCommands`, NOT Reactive) wrapping `git` via Bun.spawn. Methods return raw stdout: statusPorcelainV2Branch(cwd), diffNameStatus(cwd,{cached?}), log({cwd,branch?,limit,cursor?}) using `--format` + `--date=iso-strict`, show(cwd,ref), branchShowCurrent(cwd), stage(cwd,paths[]), unstage(cwd,paths[]). Never throw into callers on nonzero exit — return {code,stdout,stderr}.
- git.parsers.ts — pure functions: parse `--porcelain=v2 --branch` into {branch,head,staged[],unstaged[],untracked[]} with XY codes; parse log into compact CommitRecord[] {sha,shortSha,author,dateIso,subject,refs[]}. No reactive objects.
- GitRepository.ts — Reactive model. ref-getters: branch, head, staged, unstaged, untracked, historyPage (compact CommitRecord[]), refreshing, lastRefreshAt, error. refresh() async + NON-BLOCKING + supersede stale via a monotonic refresh id (discard results from an older id). stage/unstage/stageAll/unstageAll then refresh. loadHistory({branch,limit,cursor}) paged. Reads GitCommands + parsers late.
- GitStatus.ts, GitHistory.ts — thin reactive views over GitRepository, OR fold into GitRepository if cleaner (keep minimal). History MUST be paged + virtualized-ready (compact records, no rich model per commit).
- GitWatcher.ts — plain class, watches working dir (fs watch), debounces storms, triggers GitRepository.refresh(); disposable.
Load-bearing: async/non-blocking/stale-supersede; compact virtualized history. Tests: parser units (porcelain v2 + log); refresh supersede (older id discarded); stage/unstage transitions against a temp `git init` fixture created in the test.
