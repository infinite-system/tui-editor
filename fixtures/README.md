# Fixture Project — a deliberately very long first line that would soft-wrap in a 120-column terminal if the editor pane ever wrapped, which it must never do (regression: gutter desync)
A tiny project for driving the TUI under tmux.

- `src/greeter.ts` — a class with a wide char and an emoji in a comment.
- `data.json` — sample data.
